import React from 'react';
import { AddList } from '../../common';

const CommunityPost = () => {
  // 리액트 스피너 적용 = 사진 업로드에 대한 로딩 시간 지정
  // object-fit:cover;

  return <AddList />;
};

export default CommunityPost;

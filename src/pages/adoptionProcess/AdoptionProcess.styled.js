import styled from 'styled-components';

export const Layout = styled.div`
  width: 100%;
  max-width: var(--global-inner);
  margin: 0 auto;
  font-size: 18px;
  line-height: 1.6;
`;

export const ProcessWrapper = styled.p``;

export const IndexWrapper = styled.p`
  margin-top: 52px;
`;
